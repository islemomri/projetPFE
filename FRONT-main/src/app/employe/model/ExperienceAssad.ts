export interface ExperienceAssad {
  id?: number; // L'ID peut être optionnel lors de l'ajout
  poste: string;
  dateDebut: string;
  dateFin: string;
  direction: string;
  modeAffectation: string;
}
